node_modules
dist
coverage
.DS_Store
test-results
playwright-report
.worktrees
# !Klein runtime workspace state for this repo-as-a-project (board/config/sessions/CRDT +
# generated plan artifacts) — per-machine generated data, never commit.
.nklein/
# Isolated data + home dir for `./start.sh --test` (its own ~/.nklein board/config + any npm cache — throwaway).
.nklein-test-home/
# Per-machine Claude Code settings and agent worktrees are local runtime state. Keep tracked
# config such as .claude/commands/ under version control while making cleanliness independent
# of a developer's global Git excludes file.
.claude/settings.local.json
.claude/worktrees/
.sentry-artifacts
.env
.env.*
!.env.example
web-ui/.env
web-ui/.env.*
!web-ui/.env.example
packages/desktop/out
packages/desktop/cli
packages/desktop/*.tgz
*.code-workspace

# Generated desktop bundles (regenerated by build scripts; never commit)
/disconnected.html
/runtime-orchestrator.js
/kanban-*.tgz
packages/desktop/main.js
packages/desktop/runtime-child.js
packages/desktop/runtime-child-env.js
packages/desktop/desktop-preflight.js
packages/desktop/cli
packages/llm-simulator/node_modules

# §13d reflection-loop capture campaigns — per-run artifacts, regeneratable (scripts/capture-real-eval.mts)
/captures/
.real-runs/
benchmark-harness/.venv/
benchmark-harness/harbor/
benchmark-harness/swebench-live/
__pycache__/
screenlog.*
