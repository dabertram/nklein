# Integrations registry

> The single place that tracks **every external thing !Klein integrates** — engines, model hosts, CLIs, MCP servers,
> services. Maintained per the §4A rule ("web-search the ecosystem; integrate properly rather than reinvent"): whenever
> we adopt, evaluate, or drop an integration, **update this table** (name · what · why · status · license · #1 egress
> posture · where it's wired). Keep it honest — `partial`/`evaluating` are first-class statuses, not failures.

## Status legend

`integrated` = wired + in use · `partial` = some pieces built, wiring incomplete · `evaluating` = under assessment, not
adopted · `planned` = decided, not started · `dropped` = evaluated + rejected (say why).

## Registry

| integration | what it is | status | license | #1 / egress | where it's wired |
|---|---|---|:-:|---|---|
| **Cline SDK** | the vendored agent engine (`@cline/*`) — the loop/tools/streaming base | `integrated` | Apache-2.0 | local; cloud code present-but-disabled | `vendor/cline-sdk/` (built from source); §4A "Cline SDK" |
| **LM Studio** | local model host — OpenAI-compat `/v1` + native `/api/v0`,`/api/v1` + the `lms` CLI | `integrated` | proprietary (local app) | local-only (`:1234`, or LAN-linked machines) | `lmstudio-loaded-models.ts`, `lms-model-runner`, the provider service; §5.AN maps the full API surface |
| **llmfit** | LLM fit/speed/quality scorer + ~4757-model HF DB (`fit`/`recommend`/`search`/`info --json`) | `partial` | MIT | local CLI via `uvx` (no install); HF-DB refresh is OUTBOUND → egress-gated | `llmfit-adapter.ts` (parse), `llmfit-runner.ts` (`uvx llmfit`), `model-load-headroom.ts` (fit gate), `llmfit-roster.ts` (roster), `llmfit-capability-prior.ts` (cold-start prior bridge). The decompose path now feeds a **cold-start prior** into `buildLoadedModelRoutingCandidates` so a never-observed loaded model is ranked by its card — but that prior currently comes from the **§5.AL catalog** (`build-decomposition-routing-candidates.ts` `catalogCapabilityPrior`), fast + always-available. **PRIOR CHAINING DONE (2026-07-01, 473f718d):** llmfit's fit score now chains *ahead* of the catalog — `resolveLoadedModelProfile` takes an injected `llmfitPrior` (llmfit score > catalog verdict > default), and `build-decomposition-routing-candidates.ts` resolves it behind `NKLEIN_LLMFIT_PRIOR` from a process-cached `llmfitRecommend` (run once, not per-decompose). OFF by default ⇒ no `uvx`/egress; the runtime path stays local. Bumps this integration from `partial` toward `integrated` on the SELECTION axis. **PENDING:** the roster orchestration (opt-in load control) + **catalog-freshness check** (user, 2026-07-01): periodically check whether a newer llmfit model DB is available and let the user trigger the update — the LLM landscape moves fast, so a stale ~4757-model snapshot silently rots. The refresh is OUTBOUND (HF DB) ⇒ **egress-gated + opt-in** per prime-directive #1, never auto-applied; only the *check* + a user-confirmed update |
| **uv / uvx** | Python runner — runs llmfit ephemerally from the uv cache (no permanent install) | `integrated` | Apache-2.0/MIT | local; first run fetches llmfit from PyPI (one-time) | `llmfit-runner.ts` default `{ bin: "uvx", prefixArgs: ["llmfit"] }` |
| **nomic-embed-text v1.5** | local embedding model for the code index | `integrated` | Apache-2.0 | local (served by LM Studio) | the code-embedding provider; `nklein-code-index.ts` |
| **MCP servers (generic)** | user-configured Model-Context-Protocol tool servers the agent can call | `integrated` (host) | per-server | per-server (HTTPS MCP gated by §5.L / the tool-capability manifest) | `nklein-mcp-runtime-service.ts`; the runtime-settings MCP panel |
| **codebase-memory-mcp** | MCP server ([DeusData](https://github.com/DeusData/codebase-memory-mcp)) for codebase memory / symbol localization | `partial` | MIT | **local-only** (100% offline: bundled `nomic-embed-code` embeddings + local SQLite cache, no cloud/API keys) | **EVALUATED 2026-07-01 (README) → ADOPT as the §5.B `LocalizationProvider` backing.** Fits prime-directive #1 (fully offline) and exposes exactly the port's lookups: symbol/def (`search_graph`), import edges (`IMPORTS`), call graph (`trace_path`/`CALLS`), file/line spans (`get_code_snippet`), plus `semantic_query` (embeddings) + BM25 FTS. Indexes via tree-sitter AST → SQLite (`~/.cache/codebase-memory-mcp/`); README claims ~99% token reduction vs grep (the small-LLM benefit). Maturity metrics in the README (stars/tests/SLSA) are reported-but-unverified. **ADAPTER BUILT 2026-07-01:** the MCP-backed `LocalizationProvider` now exists behind the port — `createMcpLocalizationProvider(callMcpTool)` in [`src/core/mcp-localization-provider.ts`](../../src/core/mcp-localization-provider.ts), **pure over an INJECTED `callMcpTool`** (calls `search_graph`, maps result nodes → `LocalizationHit`; defensive over the undocumented result envelope — tolerates missing fields, never throws), fake-tested (13 cases) → bumps this to `partial`. **PENDING (wiring incomplete):** inject a *real* MCP client into `callMcpTool` (via `nklein-mcp-runtime-service.ts`) AFTER the binary is run/vetted inside the strict-local Docker sandbox — the adapter's real-schema correctness (exact `search_graph`/`get_code_snippet` output shapes, undocumented upstream) is validated against the live tool then. The native code-index fallback stays the "only-if-this-regresses" path, not the default. **RESEARCH-VERIFIED 2026-07-01 (live web, [repo](https://github.com/DeusData/codebase-memory-mcp)):** now `v0.6.0`, MIT, **~8.6k GitHub stars** (maturity is now *verified*, not just README-claimed), single static binary / zero deps / 158 languages / **14 MCP tools** (search/trace/architecture/impact/Cypher `query_graph`/dead-code/cross-service HTTP/ADR) / bundled `nomic-embed-code` + BM25 FTS, **100% local, no telemetry** — confirms ADOPT. As the §5.B `LocalizationProvider` *backing* it is runtime-called (not model-driven), so it benefits EVERY model (it *cuts* tokens ~99%, which helps small models most) — no per-model gate needed. Remaining work is the real MCP-client wiring. |
| **sequential-thinking MCP** | official `@modelcontextprotocol/server-sequential-thinking` — a structured step-by-step reasoning scratchpad (one `sequential_thinking` tool: `thought`/`nextThoughtNeeded`/`thoughtNumber`/`totalThoughts` + revision/branch fields) | `evaluating` (→ adopt-**gated**) | MIT | **local-only** — no egress, pure in-process reasoning log; set `DISABLE_THOUGHT_LOGGING=true` to silence the stderr log | **RESEARCHED 2026-07-01.** stdio via `npx -y @modelcontextprotocol/server-sequential-thinking` (or the `mcp/sequentialthinking` Docker image). **"For models where it fits" is the ENTIRE point here:** it helps models WITHOUT native extended reasoning that can still *reliably tool-call*, on non-trivial multi-step tasks; it is **redundant/harmful for native-reasoning models** (o1/DeepSeek-R1-style → overthinking, token waste, self-verification + oscillation loops — well-documented in 2025-26 arXiv) and **loop-risky for weak tool-callers** (`needsMoreThoughts`/`isRevision` can spiral). GATE via the §5.AL capability catalog + the reasoning-channel-split signal: offer it ONLY to non-reasoning, tool-reliable models on non-trivial tasks; never to reasoning models or weak callers. PENDING: the capability gate + the tool-offer wiring. |
| **Pieces MCP (PiecesOS LTM)** | Pieces "Long-Term Memory" MCP (`ask_pieces_ltm`) — injects the USER's cross-app workflow context (IDEs / browsers / collaboration tools; ~9-month temporal roll-ups) | `dropped` (rejected 2026-07-01; revisit only as opt-in host-chat memory) | **proprietary** (PiecesOS desktop app; freemium) | can be **100% on-device** ONLY when paired with a PiecesOS *local* model + cloud features OFF (all cloud is opt-in; telemetry anon + opt-out; SOC 2 Type II; delete via `com.pieces.os`). BUT it continuously captures broad cross-app activity → a real privacy surface | **RESEARCHED 2026-07-01.** SSE transport; server URL taken from the PiecesOS menu bar; **requires the PiecesOS desktop app running on the HOST**. **PRIME-DIRECTIVE / ARCHITECTURE TENSION:** (a) proprietary *host* desktop-app dependency — the strict-local Docker sandbox (invariant #2) cannot reach host PiecesOS, so it is a **non-starter for the swarm/board/decompose agents**; (b) its "memory" is the user's *activity*, which !Klein already covers with its own `chat-memory-store` + focus chains + the agent ledger (duplication, not a gap); (c) the capture surface (browsers etc.) is a privacy concern for a repo/board-scoped coding agent. **Verdict: do NOT wire into sandboxed agents; at most an OPT-IN, OFF-by-default, local-model-only memory source for the HOST chat assistant — pending a user decision.** |

## Notes

- **Prime directive #1 (local-only):** an integration's *runtime* path must stay local; any OUTBOUND feature (llmfit's
  HF-DB refresh / leaderboard, an MCP server's network egress, online research §5.AC) is **egress-gated + opt-in**, never
  silently on. Record the posture in the table.
- **"Evaluate before building custom" (§4A):** `codebase-memory-mcp` is the live example — assess it as the localization
  backing before hand-rolling one. Same rule for any future capability: search the ecosystem first.
- **Vendored vs called:** the Cline SDK is *vendored as source* (we build it, patch deliberately — see its `NOTICE.md`
  patch ledger). llmfit/MCP/LM Studio are *called* (CLI/API) — no source in-tree.
