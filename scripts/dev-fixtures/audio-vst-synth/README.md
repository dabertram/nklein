# Psytrance VST Prototype

This fixture starts as a tiny TypeScript DSP prototype. The goal is to evolve it into a VST-style audio
plugin core for clean modern psytrance kick and bass grooves without adding dependencies.

Run tests with:

```sh
npm test
```
