/**
 * Your consumer. This file is yours: replace the stub.
 *
 * `createEventConsumer(service, options)` must return an object with `run()`.
 * See specification.md for the contract, and conformance/cases.mjs for exactly what is checked.
 */

export function createEventConsumer(_service, _options) {
	throw new Error("createEventConsumer is not implemented yet — see specification.md");
}
