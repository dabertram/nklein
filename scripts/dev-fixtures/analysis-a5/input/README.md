Evidence bundle: four handler modules and the route table that registers them, lifted from a checkout platform
before its external penetration test. `routes.mjs` is the authoritative statement of which handler is reachable by
whom. Read them; do not modify them.
