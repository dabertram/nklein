Evidence bundle: `spec-v1.md` is the specification the service was built to, with a `Contract` marker on every
requirement, and `change-request.md` is the approved change request against it. Read them; do not modify them.
