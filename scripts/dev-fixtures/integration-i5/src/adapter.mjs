/**
 * Your coordinator. This file is yours: replace the stub.
 *
 * `createOrderCoordinator({ ledger, fulfilment, sleep })` must return an object with `placeOrder(order)`.
 * See specification.md for the contract, and conformance/cases.mjs for exactly what is checked.
 */

export function createOrderCoordinator(_deps) {
	throw new Error("createOrderCoordinator is not implemented yet — see specification.md");
}
