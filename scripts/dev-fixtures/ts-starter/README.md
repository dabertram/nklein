# TypeScript starter (empty)

A minimal, domain-free TypeScript toolchain for build-from-scratch dev-test projects: `src/**/*.ts` product code,
`test/**/*.test.{js,ts}` node:test files, `npm test` runs them via `scripts/run-tests.mjs`. There is no example domain
code — build the real system per `specification.md`.
