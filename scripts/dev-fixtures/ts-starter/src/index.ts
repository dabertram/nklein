// Empty starter for a build-from-scratch dev-test project. The agent builds the real domain model, modules, and tests
// per specification.md — there is intentionally NO example domain code here (unlike smoke-ts-cli's habit-score fixture,
// which would mislead a decompose into implementing habit-score instead of the project's actual scope).

export const PROJECT_STARTED = true;
