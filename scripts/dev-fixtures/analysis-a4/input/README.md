Evidence bundle: `shipments.csv` is one month of shipment records exported from a freight platform, and
`carriers.csv` is the carrier reference table the export is supposed to agree with. Both are plain CSV — no
quoted fields, no embedded commas — with a header row. Read them; do not modify them.
