{
 "id": "bed-refactor-inventory",
 "title": "Inventory refactor (refactor bed)",
 "prompt": "Create src/inventory.ts with EXACTLY this content, then refactor it as instructed:\n\n```ts\nexport type Item = { sku: string; qty: number; price: number; tags: string[] };\nexport function processInventory(items: Item[], op: string, arg?: unknown): unknown {\n  if (op === 'total') { let t = 0; for (const i of items) { t += i.qty * i.price; } return Math.round(t * 100) / 100; }\n  else if (op === 'low') { const out: string[] = []; for (const i of items) { if (i.qty < (arg as number)) { out.push(i.sku); } } return out.sort(); }\n  else if (op === 'tagged') { const out: Item[] = []; for (const i of items) { for (const t of i.tags) { if (t === arg) { out.push(i); break; } } } return out; }\n  else if (op === 'restock') { const out: Item[] = []; for (const i of items) { if (i.qty === 0) { out.push({ ...i, qty: arg as number }); } else { out.push(i); } } return out; }\n  else { throw new Error('unknown op ' + op); }\n}\n```\n\nRefactor requirements:\n1. Replace the string-op dispatcher with four named, typed functions: totalValue(items): number, lowStock(items, threshold): string[], taggedItems(items, tag): Item[], restockEmpty(items, qty): Item[] — each preserving the EXACT current behavior (including rounding, sorting, first-match tag semantics, and non-mutation of inputs).\n2. Keep processInventory as a thin deprecated wrapper delegating to the four functions with identical behavior (same throw for unknown ops).\n3. Add test/inventory.test.ts (node:test + node:assert/strict) with behavior-pinning tests written FIRST against the original semantics: rounding to cents, sorted low-stock, break-on-first-tag (an item with the tag twice appears once), restock only qty===0, inputs never mutated (assert deep-equal on the original array after each call), and wrapper/function parity for all four ops plus the unknown-op throw.\n\nRun the tests with `node --test` (this Node runs .ts natively — do NOT use npx or install anything; the sandbox has no network) and make them all pass.\n\nAcceptance command: node --test test/inventory.test.ts",
 "specification": "Inventory refactor (refactor bed) — the prompt is self-contained; build exactly what it asks in the current directory. Acceptance: `node --test test/inventory.test.ts`.",
 "acceptanceCommand": "node --test test/inventory.test.ts"
}